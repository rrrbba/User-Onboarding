{
    "version": 2,
    "name": "project-name",
    "builds": [{ "src": "user-onboarding/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "user-onboarding/$1" }
    ]
}